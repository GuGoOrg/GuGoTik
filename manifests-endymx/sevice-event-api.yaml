apiVersion: v1
kind: Service
metadata:
  labels:
    app: gugotik-event-service
  name: gugotik-event-service
  namespace: gugotik-service-bundle
spec:
  ports:
    - name: metrics
      port: 37099
      protocol: TCP
      targetPort: 37099
  selector:
    name: gugotik-event-service
    branch: master
  type: ClusterIP